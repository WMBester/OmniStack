trigger:
  branches:
    include:
      - '*'

pr:
  branches:
    include:
      - master

stages:
- stage: BuildAndTest
  displayName: 'Build and Unit Test Execution'
  jobs:
  - template: build-and-test.yml

- stage: Stryker
  displayName: 'Mutation Testing with Stryker'
  dependsOn: []
  condition: false  # Disabled by default, run manually
  approvals:
    - name: 'Tester Approval'
      reviewers:
        - willemmorne@gmail.com 
  jobs:
  - template: stryker.yml
